const animate = () => {
  // For each particle
  // Calc force v2
  // Move particle using force
  // Reverse velocity on boundary collision
  // Then
  // Erase canvas
  // Draw particles
  // Recursively call fn
};

export default animate;
